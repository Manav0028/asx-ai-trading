# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data ingestion ────────────────────────────────────────────────────────────
yfinance>=0.2.40
feedparser>=6.0.11
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Storage ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
redis>=5.0.0

# ── AI / ML ───────────────────────────────────────────────────────────────────
anthropic>=0.40.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# ── Scheduler ─────────────────────────────────────────────────────────────────
APScheduler>=3.10.4

# ── Phase 2 — Cloud sync + Dashboard ─────────────────────────────────────────
supabase>=2.0.0              # cloud signal backup + remote dashboard data
streamlit>=1.12.0            # web dashboard (run: streamlit run dashboard/app.py)
plotly>=5.0.0                # interactive charts in dashboard
streamlit-autorefresh>=0.0.1 # 5-minute auto-refresh for dashboard

# ── Phase 3 (uncomment when ready) ───────────────────────────────────────────
# vectorbt>=0.26.0    # requires numba — use conda: conda install -c numba numba
# ib_insync>=0.9.86   # IBKR live trading
